---
import { PortableText } from 'astro-portabletext';
import PortableTextImage from '../ui/PortableTextImage.astro';
import PortableTextHighlight from '../ui/PortableTextHighlight.astro';
import PortableTextCode from '../ui/PortableTextCode.astro';

interface Props {
  content?: any[];
}

const { content = [] } = Astro.props;

const components = {
  type: {
    image: PortableTextImage,
    code: PortableTextCode,
  },
  mark: {
    highlight: PortableTextHighlight,
  },
};

const hasContent = content && content.length > 0;
---

{hasContent && (
  <section class="case-content">
    <div class="case-content__container">
      <div class="case-content__body">
        <PortableText value={content} components={components} />
      </div>
    </div>
  </section>
)}

<style>
  .case-content {
    padding: 6rem 0;
    background: var(--bg-secondary);
    overflow-x: hidden;
  }

  .case-content__container {
    width: 100%;
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .case-content__body {
    max-width: 800px;
    margin: 0 auto;
    font-family: var(--font-body);
    font-size: 1.125rem;
    font-weight: 300;
    line-height: 1.8;
    color: var(--text-secondary);
  }

  /* ---- Paragraphs ---- */
  .case-content__body :global(p) {
    margin-bottom: 1.5rem;
  }

  .case-content__body :global(p:last-child) {
    margin-bottom: 0;
  }

  /* ---- Bold / links ---- */
  .case-content__body :global(strong) {
    font-weight: 500;
    color: var(--text-primary);
  }

  .case-content__body :global(a) {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 4px;
    transition: color 0.2s;
  }

  .case-content__body :global(a:hover) {
    color: var(--accent-light);
  }

  /* ---- Lists ---- */
  .case-content__body :global(ul) {
    margin: 1.5rem 0;
    padding-left: 1.5rem;
    list-style: none;
  }

  .case-content__body :global(ol) {
    margin: 1.5rem 0;
    padding-left: 1.5rem;
    list-style: decimal;
    color: var(--accent);
  }

  .case-content__body :global(ol > li) {
    padding-left: 0.5rem;
    color: var(--text-secondary);
  }

  .case-content__body :global(li) {
    margin-bottom: 0.5rem;
  }

  .case-content__body :global(ul > li) {
    position: relative;
    padding-left: 1.25rem;
  }

  .case-content__body :global(ul > li::before) {
    content: '';
    position: absolute;
    left: 0;
    top: 0.7em;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
  }

  /* ---- Headings ---- */
  .case-content__body :global(h2),
  .case-content__body :global(h3) {
    font-family: var(--font-display);
    font-weight: 500;
    color: var(--text-primary);
    margin: 2.5rem 0 1rem;
    line-height: 1.2;
  }

  .case-content__body :global(h2) {
    font-size: 1.75rem;
  }

  .case-content__body :global(h3) {
    font-size: 1.375rem;
  }

  /* ---- Blockquote: gold border strip ---- */
  .case-content__body :global(blockquote) {
    border-left: 3px solid var(--accent);
    padding-left: 2rem;
    margin: 2.5rem 0;
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.25rem;
    font-weight: 400;
    line-height: 1.6;
    color: var(--text-primary);
  }

  .case-content__body :global(blockquote p) {
    margin-bottom: 0;
  }

  /* ---- Images: magazine breakout ---- */
  .case-content__body :global(.portable-text-image) {
    width: 100vw;
    max-width: 1440px;
    margin: 2.5rem 0;
    position: relative;
    left: 50%;
    transform: translateX(-50%);
  }

  .case-content__body :global(.portable-text-image img) {
    width: 100%;
    height: auto;
    border: 1px solid var(--border-light);
  }

  .case-content__body :global(.portable-text-image figcaption) {
    font-size: 0.8125rem;
    color: var(--text-muted);
    margin-top: 0.75rem;
    font-style: italic;
  }

  /* ---- Code blocks ---- */
  .case-content__body :global(.portable-text-code) {
    width: 100vw;
    max-width: 1000px;
    position: relative;
    left: 50%;
    transform: translateX(-50%);
  }

  /* ---- Responsive ---- */
  @media (max-width: 768px) {
    .case-content {
      padding: 4rem 0;
    }

    .case-content__body {
      font-size: 1rem;
    }

    .case-content__body :global(blockquote) {
      padding-left: 1.5rem;
      font-size: 1.125rem;
    }

    .case-content__body :global(.portable-text-image),
    .case-content__body :global(.portable-text-code) {
      width: 100%;
      left: 0;
      transform: none;
    }
  }

  @media (max-width: 480px) {
    .case-content {
      padding: 3rem 0;
    }

    .case-content__container {
      padding: 0 1rem;
    }
  }

  @media (min-width: 1024px) {
    .case-content__container {
      padding-left: 3rem;
      padding-right: 3rem;
    }
  }
</style>
