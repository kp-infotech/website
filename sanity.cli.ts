import { defineCliConfig } from 'sanity/cli';

export default defineCliConfig({
  api: {
    projectId: '5rux0mv2',
    dataset: 'production',
  },
  deployment: {
    appId: 'nrhngkka32o4vdxu667lovc8',
  },
});
